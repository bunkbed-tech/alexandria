services:
  postgres:
    image: postgres:15
    container_name: postgres
    env_file: .env
    healthcheck:
      test: [ CMD, pg_isready, -U, $POSTGRES_USER, -d, $POSTGRES_DB ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 5s
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - postgres-logs:/var/lib/postgresql/pg_log
    ports:
      - 5432:5432
  migrate:
    build:
      context: backend
      dockerfile: migrate.Dockerfile
    container_name: migrate
    environment:
      DATABASE_URL: postgresql://$POSTGRES_USER:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/$POSTGRES_DB
    command: [ cargo, sqlx, migrate, run ]
    depends_on:
      postgres:
        condition: service_healthy
  backend:
    image: rust:1.83
    container_name: backend
    environment:
      DATABASE_URL: postgresql://$POSTGRES_USER:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/$POSTGRES_DB
      ALEXANDRIA_IGDB_CLIENT_ID: ${ALEXANDRIA_IGDB_CLIENT_ID}
      ALEXANDRIA_IGDB_CLIENT_SECRET: ${ALEXANDRIA_IGDB_CLIENT_SECRET}
    working_dir: /app
    command: [ cargo, run ]
    depends_on:
      migrate:
        condition: service_completed_successfully
    ports:
      - 8080:8080
    volumes:
      - ./backend:/app
  # frontend:
  # cli:
  # desktop:

  # TODO fix dummy data script
  # dummy_data:
  #   image: postgres:15
  #   container_name: dummy_data
  #   working_dir: /data
  #   env_file: .env
  #   environment:
  #     PGPASSWORD: $POSTGRES_PASSWORD
  #   volumes:
  #     - ./shared/models/tests/data:/data
  #   command: [ psql, -h, $POSTGRES_HOST, -U, $POSTGRES_USER, -d, $POSTGRES_DB, -f, dummy.sql]
  #   depends_on:
  #     - migrate
volumes:
  postgres-data:
  postgres-logs:
